package allofhealth.messenger.config;

import allofhealth.messenger.auth.UserAuthRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.core.userdetails.*;

@Configuration
@RequiredArgsConstructor
public class SecurityConfig {

    private final UserAuthRepository userAuthRepository;

    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
    // ??????????????
//    @Bean
//    public ReactiveUserDetailsService userDetailsService(){
//        UserDetails userDetails = (UserDetails) username -> userAuthRepository.findById(username);
//        MapReactiveUserDetailsService userDetailsService = username -> userAuthRepository.findById(username)
//                .orElseThrow(() -> new UsernameNotFoundException("User not found for username : " + username));;
//        return userDetailsService;
//    }
}
